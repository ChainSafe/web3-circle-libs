modules = ["nodejs-20", "web"]
run = "npm run dev"
[nix]
channel = "stable-24_05"

[deployment]
deploymentTarget = "cloudrun"
run = "cd packages/circle-react-elements && yarn build && cd ../circle-demo-webapp && yarn dev"

[[ports]]
localPort = 3000
externalPort = 80
